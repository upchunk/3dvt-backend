services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    image: "${BACKEND_IMAGE}"
    command: python manage.py runserver 0.0.0.0:8000
    expose:
      - 8000
    env_file:
      - .env
